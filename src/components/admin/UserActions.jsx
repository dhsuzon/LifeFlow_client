"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { toast } from "react-toastify"; import { updateUserAccess } from "@/lib/actions/admin";
export default function UserActions({user}){const[busy,setBusy]=useState(false);const router=useRouter();const run=async update=>{setBusy(true);const r=await updateUserAccess(user.id,update);setBusy(false);r.success?toast.success("User updated."):toast.error(r.error);if(r.success)router.refresh()};return <div className="flex min-w-48 flex-wrap gap-2"><button disabled={busy} onClick={()=>run({status:user.status==="active"?"blocked":"active"})} className="rounded bg-amber-600 px-2 py-1 text-xs font-bold text-white">{user.status==="active"?"Block":"Unblock"}</button>{user.role==="donor"&&<button disabled={busy} onClick={()=>run({role:"volunteer"})} className="rounded bg-blue-600 px-2 py-1 text-xs font-bold text-white">Make Volunteer</button>}{user.role!=="admin"&&<button disabled={busy} onClick={()=>run({role:"admin"})} className="rounded bg-red-600 px-2 py-1 text-xs font-bold text-white">Make Admin</button>}</div>}
