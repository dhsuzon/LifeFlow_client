"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { toast } from "react-toastify"; import { setManagedDonationStatus } from "@/lib/actions/platform"; import RequestActions from "@/components/donation-request/RequestActions";
export default function ManagedRequestActions({request,role}){const[busy,setBusy]=useState(false);const router=useRouter();const change=async e=>{setBusy(true);const r=await setManagedDonationStatus(request.id,e.target.value);setBusy(false);r.success?toast.success("Status updated."):toast.error(r.error);if(r.success)router.refresh()};if(role==="admin")return <RequestActions requestId={request.id} status={request.donationStatus}/>;return <select disabled={busy} value={request.donationStatus} onChange={change} className="rounded border bg-white px-2 py-1 text-xs dark:border-gray-700 dark:bg-gray-800 dark:text-white">{["pending","inprogress","done","canceled"].map(s=><option key={s}>{s}</option>)}</select>}
