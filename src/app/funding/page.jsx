import { redirect } from "next/navigation"; import { getFundingRecords } from "@/lib/actions/funding"; import GiveFundButton from "@/components/funding/GiveFundButton";
export default async function FundingPage({searchParams}){const records=await getFundingRecords();if(!records)redirect("/auth/login");const query=await searchParams;return <main className="mx-auto w-full max-w-5xl px-4 py-12"><div className="flex items-center justify-between"><div><h1 className="text-3xl font-extrabold dark:text-white">Organization Funding</h1><p className="mt-1 text-gray-500">Community contributions to support blood donation activities.</p></div><GiveFundButton/></div>{query.payment==="success"&&<p className="mt-5 rounded-lg bg-emerald-100 p-3 text-emerald-800">Payment completed. The record will appear after Stripe confirms it.</p>}<div className="mt-8 overflow-x-auto rounded-2xl border bg-white dark:border-gray-800 dark:bg-gray-900"><table className="w-full min-w-[600px] text-left"><thead className="bg-gray-100 dark:bg-gray-800 dark:text-gray-300"><tr><th className="px-5 py-4">Contributor</th><th className="px-5 py-4">Amount</th><th className="px-5 py-4">Funding Date</th></tr></thead><tbody className="divide-y dark:divide-gray-800">{records.map(f=><tr key={f.id} className="dark:text-gray-200"><td className="px-5 py-4 font-semibold">{f.userName}</td><td className="px-5 py-4">${Number(f.amount).toFixed(2)}</td><td className="px-5 py-4">{f.fundingDate?new Date(f.fundingDate).toLocaleDateString("en-GB"):"—"}</td></tr>)}</tbody></table></div></main>}
