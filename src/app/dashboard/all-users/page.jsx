import Image from "next/image"; import Link from "next/link"; import { notFound } from "next/navigation"; import { getAllUsers } from "@/lib/actions/admin"; import UserActions from "@/components/admin/UserActions";
export default async function AllUsersPage({searchParams}){const q=await searchParams;const result=await getAllUsers({status:q.status,page:q.page});if(!result)notFound();return <section><div className="mb-6 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-3xl font-extrabold dark:text-white">All Users</h1><p className="text-sm text-gray-500">Total users: {result.pagination.totalItems}</p></div><form className="flex gap-2"><select name="status" defaultValue={result.selectedStatus} className="rounded-lg border bg-white px-3 py-2 dark:border-gray-700 dark:bg-gray-800 dark:text-white"><option value="all">All Statuses</option><option value="active">Active</option><option value="blocked">Blocked</option></select><button className="rounded-lg bg-danger px-4 py-2 font-bold text-white">Filter</button></form></div><div className="overflow-x-auto rounded-2xl border bg-white dark:border-gray-800 dark:bg-gray-900"><table className="w-full min-w-[900px] text-left text-sm"><thead className="bg-gray-100 text-xs uppercase dark:bg-gray-800 dark:text-gray-300"><tr>{["Avatar","Name","Email","Role","Status","Actions"].map(x=><th key={x} className="px-4 py-3">{x}</th>)}</tr></thead><tbody className="divide-y dark:divide-gray-800">{result.users.map(u=><tr key={u.id} className="dark:text-gray-200"><td className="px-4 py-3"><Image src={u.image||"/images/default_profile.jpg"} alt={u.name} width={40} height={40} className="h-10 w-10 rounded-full object-cover"/></td><td className="px-4 py-3 font-semibold">{u.name}</td><td className="px-4 py-3">{u.email}</td><td className="px-4 py-3 capitalize">{u.role}</td><td className="px-4 py-3 capitalize">{u.status}</td><td className="px-4 py-3"><UserActions user={u}/></td></tr>)}</tbody></table></div>{result.pagination.totalPages>1&&<div className="mt-5 flex justify-center gap-2">{Array.from({length:result.pagination.totalPages},(_,i)=><Link key={i} href={`?status=${result.selectedStatus}&page=${i+1}`} className={`rounded border px-3 py-2 ${result.pagination.currentPage===i+1?"bg-danger text-white":"dark:border-gray-700 dark:text-white"}`}>{i+1}</Link>)}</div>}</section>}
